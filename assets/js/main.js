// console.log('haha!');


// let to_render_list = document.getElementsByClassName("maf-eq");
// to_render_list = Array.prototype.slice.call(to_render_list);

// //console.log(to_render_list);

// to_render_list.forEach(element => {
//     console.log(element.innerHTML);

//     const rendered_HTML = katex.renderToString(element.innerHTML, {
//         throwOnError: false
//     });
//     element.parentNode.replaceChild(rendered_HTML, element);
// });